import React from 'react';
import Home from "./core/Home";
import './scss/App.scss';

// App serves as a wrapper component route logic is in the Router component
function App() {
  return (
   <Home />
  );
}

export default App;
